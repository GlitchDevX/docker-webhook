[project]
name = "docker-redeploy-webhook"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "docker>=7.1.0",
    "fastapi[standard]>=0.115.13",
    "pyinstaller>=6.14.1",
    "pytest>=8.4.1",
]
